# объявление функции
def merge(a, list):
    for i in range(n):
        for j in range()
    a.sort()
    return a

# считываем данные
numbers1 = [int(c) for c in input().split()]
numbers2 = [int(c) for c in input().split()]

# вызываем функцию
print(merge(n, numbers))